/*const mongoose = require('mongoose')
const {Schema} = mongoose; // =const Schema = mongoose.Schema;  destructuring


const herbpicSchema = new Schema ({
    plantId: {type: Number},
    dateSent: {type: Date, default:Date.now()},
    pic: {
        data: Buffer,
        contentType:String
    }
    
})

mongoose.model('herbpics',herbpicSchema);*/